[Unit]
Description=Gunicorn for salt api
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/gunicorn -w 4 --bind localhost:9000 salt.netapi.rest_cherrypy.wsgi

TimeoutStopSec=3

[Install]
WantedBy=multi-user.target
